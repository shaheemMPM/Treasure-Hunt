import React from 'react';
import WindMillLoading from './WindMillLoading';

const Loading = (props) => {
    return ( 
        <WindMillLoading color="#3B0D00" size="large"/>
    );
}

export default Loading;